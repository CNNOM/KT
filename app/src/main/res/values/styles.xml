<resources>
    <!-- Уже существующие стили -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:textColorPrimary">@color/colorTextPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextSecondary</item>
    </style>

    <!-- Новая тема для SplashActivity -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/purple_500</item> <!-- Фиолетовый фон -->
        <item name="android:textColorPrimary">@color/white</item> <!-- Белый текст -->
    </style>

    <style name="Theme.Group_projectvstu.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.Group_projectvstu.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>